#include "main.h"
/**
 * _isdigit - function that checks for a digit (0 through 9).
 * @c: inter thats to be checked
 * Return: 1 if c is a digit, and 0 is not
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
