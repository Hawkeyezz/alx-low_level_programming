#include "main.h"
/**
 * mul - function that checks for a digit (0 with).
 * @a: a - first variable
 * @b: b - second variable
 * Return: Must be 0
 */
int mul(int a, int b)
{
int mul;

mul = a * b;
return (mul);
}
