#include "main.h"
/**
 * print_numbers - a function to check for a digit (0 to 9).
 *
 * Return: must be 0
 */
void print_numbers(void)
{
int c;

for (c = 48; c < 58; c++)
{
_putchar(c);
}
_putchar('\n');
}
