#include "main.h"                            
/**
 * _isdigit - let's check for a digit (0 through 9)
 * @c: integer to be checked
 * Return: 1 if c is a digit, and 0 if it's not.
 */
int _isdigit(int c)
{
                return (c >= '0' && c <= '9');
}
