#include <stdio.h>
#include <stdlib.h>

/**
 * main - A function to print the name of a file
 * @a - file compiled from
 * Return: Must be 0 (Success)
 */

int main(void)
{
printf("%s\n", __FILE__);
return (0);
}
