#include "main.h"
/**
 * _islower - a function to check for lowercase alphabet
 * @c: alphabet to check
 * Return: 1 if c is lowercase, if its's not 0
 */
int _islower(int c)
{
                return (c >= 'a' && c <= 'z');
}
