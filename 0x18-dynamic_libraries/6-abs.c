#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @n: the integer to check
 * Return: the correct value of the integer
 */
int _abs(int n)
{
                if (n >= 0)
                {
                            return (n);
                }
                return (-n);
}
